name: inventory_management_app
description: "A new Flutter project."
publish_to: 'none' # Private package, will not publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # iOS style icons
  cupertino_icons: ^1.0.8

  # State management and routing
  get: ^4.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Recommended lints for Flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Add assets here if needed
  # assets:
  #   - assets/images/

  # Add custom fonts here if needed
  # fonts:
  #   - family: CustomFont
  #     fonts:
  #       - asset: fonts/CustomFont-Regular.ttf
  #       - asset: fonts/CustomFont-Bold.ttf
  #         weight: 700
